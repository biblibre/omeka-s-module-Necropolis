---
kind: pipeline
name: omeka:4.1.1 php:8.2 mariadb:11.4 cypress
services:
- name: omeka
  build:
    context: .
    args:
      OMEKA_S_VERSION: ${OMEKA_S_VERSION:-"4.1.1"}
      PHP_VERSION: ${PHP_VERSION:-"8.2"}
    target: prod
  restart: "no"
  env_file: ".env"
  depends_on:
    - db
- name: db
  image: mariadb:${MARIADB_VERSION:-11.4}
  restart: "no"
  environment:
    MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    MYSQL_DATABASE: ${MYSQL_DATABASE}
    MYSQL_USER: ${MYSQL_USER}
    MYSQL_PASSWORD: ${MYSQL_PASSWORD}
steps:
- commands:
  - bash -c "sleep 30 && npx cypress run || base64 -w0 'cypress/screenshots/necropolis.cy.js/Necropolis (failed).png' && return 1"
  image: cypress/included
  name: cypress
workspace:
  path: omeka-s/modules/Necropolis
---
kind: signature
hmac: b6bbd94e7d1e40079e546386dca31b3ccc87b0439d315261be5a7b950415b7d2

...
